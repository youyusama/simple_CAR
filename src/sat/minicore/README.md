# minicore

Lightweight standalone SAT solver binary for DIMACS CNF files.

Build with the local makefile:

```sh
make
```

Run on a CNF file:

```sh
./minicore input.cnf
./minicore -v --model input.cnf
```
